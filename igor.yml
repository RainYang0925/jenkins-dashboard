name: "Jenkins Dashboard"
description: "As the name says, it shows the status of Jenkins jobs for your team."
owner:
  email: "jeremy.dagorn@prezi.com"
# Build section is used for your Jenkins jobs. By default it creates Jenkins Dashboard-build # EDITME
build:
  test-and-build:
    run: |
      ./scripts/test.sh ${BUILD_NUMBER}-${GIT_COMMIT}
      # ./scripts/build ${BUILD_NUMBER}-${GIT_COMMIT}
    pinned: precise
# Deploy section is used by igor cook, it will use this information to setup Chef.
deploy:
  app:
    run: "scripts/run_Jenkins Dashboard.sh"
    port: "8000"
generated_by: igor 2.0.4-AwesomeSergei