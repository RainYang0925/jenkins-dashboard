name: "jenkinsdashboard"
description: "A better dashboard for jenkins"
owner:
  email: "simone.fonda@prezi.com"
  hipchat: "koi"
# Build section is used for your Jenkins jobs. By default it creates jenkinsdashboard-build # EDITME
build:
  test-and-build:
    run: |
      run_docker.sh -x -d dock.prezi.com:5000/boxfish -- ./scripts/build ${BUILD_NUMBER}-${GIT_COMMIT}
      ./scripts/uploadAndRelease ${BUILD_NUMBER}-${GIT_COMMIT}
      # ./scripts/test.sh ${BUILD_NUMBER}-${GIT_COMMIT}
      # ./scripts/build ${BUILD_NUMBER}-${GIT_COMMIT}
    pinned: precise
# Deploy section is used by igor cook, it will use this information to setup Chef.
deploy:
  app:
    run: "scripts/run_jenkinsdashboard.sh"
    port: "8000"
generated_by: igor 2.0.16-YOLOJENKINS